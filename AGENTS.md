# AGENTS.md 通用模板

> 用途：这是给具体仓库落地前的起始模板，不建议长期原样使用。
> 要求：拿到项目后，必须结合该项目的真实技术栈、目录结构、命令入口和协作方式裁剪成实用版。

## 1. 回答语言与规则优先级

- 默认使用中文回答，除非用户明确要求英文。
- 规则优先级从高到低如下：
  1. 用户当前消息里的明确要求
  2. 当前仓库根目录或更近目录下的 `AGENTS.md`
  3. 当前仓库的 `docs/`、设计文档、贡献指南
  4. 当前仓库的 `README.md`、脚本说明、示例
  5. 本模板
  6. skills 或外部通用提示词
- 不得因为存在 skill 而覆盖当前仓库的具体规则和上下文。

## 2. 冷启动要求

- 在修改任何代码前，先阅读与任务直接相关的上下文：
  - 当前仓库的 `AGENTS.md`
  - `README.md`
  - `docs/`
  - 构建文件与依赖清单，如 `package.json`、`pyproject.toml`、`go.mod`、`Cargo.toml`、`docker-compose.yml`
  - 与目标功能直接相关的代码、测试、示例和配置
- 在第一次进入一个项目时，至少确认以下事实：
  - 项目是什么类型：Web、CLI、库、数据项目、单体、Monorepo
  - 主技术栈和包管理器是什么
  - 启动、构建、测试、格式化、数据库迁移的实际命令是什么
  - 主要目录边界和模块职责是什么
  - 现有代码风格、错误处理方式、测试组织方式是什么
- 如果当前仓库尚未有项目化 `AGENTS.md`，应基于本模板产出一个贴合该仓库的版本，而不是直接照搬本模板。
- 具体执行流程见同目录下的 [冷启动.md](./冷启动.md)。

## 3. 什么时候查看 skills

- 默认不是先看 skill，再做事；而是先看仓库上下文，再判断是否命中某个 skill。
- 只有在以下情况才应主动查看对应 `SKILL.md`：
  - 用户明确点名某个 skill 或明确要求按某种专用工作流执行
  - 任务明显属于某个专门领域，例如 `.docx`、`.pptx`、`.pdf`、表格、前端视觉设计、浏览器联调、特定厂商 API 文档
  - 该 skill 提供的工具链或交付流程会实质性提高正确性，而不是只增加冗余约束
- 普通代码修复、接口开发、类型调整、测试补充、少量页面逻辑修改，默认不需要为了“用 skill”而用 skill。
- 如果一个项目希望固定某些任务必须走某些 skill，应把映射关系写入该项目自己的 `AGENTS.md`。

## 4. 变更原则

- 优先做最小可验证修改，不擅自做大规模无关重构。
- 修改前先理解现有实现，不要基于猜测直接改。
- 优先复用现有模块、已有约定和成熟依赖，不重复造轮子。
- 不要引入只为临时修复而存在的复杂中间状态、隐式分支或难以维护的特殊逻辑。
- 不要无故改变公开接口、目录边界、命名体系和数据流方向。
- 不要随意覆盖或回退用户已有修改；如果发现意外变动且无法确认来源，应暂停并说明情况。

## 5. 文档、测试与示例同步

- 每次修改后，都要检查是否需要同步更新：
  - `README.md`
  - `docs/`
  - 注释和示例
  - 测试用例
  - `.env.example`、配置说明、脚本说明
- 如果仓库已有测试或校验命令，应优先运行与本次改动最相关的最小验证集。
- 如果无法运行验证，必须明确说明原因、已做的静态检查以及剩余风险。

## 6. 审查与安全基线

- 当用户要求“review”或“审查”时，默认进入代码审查模式：
  - 先给问题清单，再给总结
  - 优先指出 bug、回归风险、边界缺失、安全问题和测试空洞
  - 结论必须尽量落到具体文件和位置
- 默认假设输入不天然可信，重点检查：
  - 请求参数
  - Header、Cookie、JWT、Session
  - 数据库既有脏数据
  - 外部 API 返回值
  - 缓存与异步消息
- 如果任务本身是安全审计或信任边界梳理，可进一步参考：
  - [代码审计.md](./代码审计.md)
  - [强前置条件约束.md](./强前置条件约束.md)

## 7. 代码组织与架构约束

- 新增代码时应尽量贴近仓库现有层次，不要凭空套用外部“标准结构”。
- 只有在仓库缺少明确结构、且用户目标确实需要时，才参考：
  - [代码组织.md](./代码组织.md)
  - [通用项目架构模板.md](./通用项目架构模板.md)
- 参考的目的是帮助落地，不是强行把所有项目改造成统一模板。

## 8. 输出要求

- 最终回复至少应说明：
  - 改了什么
  - 为什么这样改
  - 如何验证
  - 还剩哪些风险或未覆盖项
- 如果这是一次项目冷启动，还应额外说明：
  - 从哪些事实中推导出项目约束
  - 模板被裁剪掉了哪些不适用项
  - 为了让仓库契约落地，顺手补了哪些文档或脚本说明
