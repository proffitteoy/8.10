# AGENTS.md

## 1. 项目定位与规则优先级

- 默认使用中文回答，除非用户明确要求其他语言。
- 本仓库是 2026 年华数杯 B 题“VLSI 布图规划设计”的数学建模工作区，当前阶段以赛题材料、基准数据和参考论文为主，不是已有可运行代码的应用仓库。
- 规则优先级：用户当前要求 > 本文件 > `README.md` 与 `docs/` > 赛题原文与附件说明 > 通用 skill。
- 涉及目标函数、约束、数据语义时，以 `B题 VLSI布图规划设计.pdf` 为准；参考论文只提供方法依据，不得覆盖赛题定义。

## 2. 当前仓库事实

- `B题 VLSI布图规划设计.pdf`：赛题原文，共 4 个问题。
- `ref/`：参考论文与参考文献，其中包含 B\*-Tree 与 Fast-SA 原论文。
- `附件/`：`n100`、`n200`、`n300` 三组原始基准数据，每组包含 `.blocks`、`.nets`、`.pl`。
- `docs/`：协作、冷启动和专项任务说明。
- 当前已建立 Python 3.12 + uv + OR-Tools 的问题 1 直接优化基线；仓库为 Git 仓库，但工作区可能包含尚未提交的建模和文献整理改动。

## 3. 原始材料与数据边界

- 赛题 PDF、参考论文和 `附件/` 均视为只读原始材料。不得覆盖、重命名、格式化或把运行结果写回其中。
- `.blocks` 给出可放置 HardBlock 及固定 Terminal；HardBlock 可旋转 90°，Terminal 不参与面积和重叠约束。
- `.nets` 以 `NetDegree` 分组给出线网节点；`.pl` 给出 Terminal 的固定坐标。
- HPWL 按每条线网所有引脚坐标的最小外接矩形半周长求和。HardBlock 引脚位于模块几何中心，Terminal 使用 `.pl` 坐标。
- 解析器必须验证文件头声明数量、实际记录数量、`NetDegree` 总和、节点引用和 Terminal 坐标集合；遇到不一致应明确失败，不得静默跳过。

## 4. 四个问题的建模边界

- 问题 1：芯片轮廓不固定、忽略线网；以轮廓面积最小为首要目标，同面积时长宽比越接近 1 越优。必须保证模块不重叠。
- 问题 2：死区比例固定为 0.15，芯片轮廓为由模块总面积确定的正方形；在不重叠、不越界前提下最小化总 HPWL。
- 问题 3：分别求三组数据存在可行布局的最小死区比例，再在对应最小死区下求解问题 2 的 HPWL。必须区分“找到的最好值”和“已证明的最小值”。
- 问题 4：支持 L 型、T 型和矩形模块及 0°、90°、180°、270° 旋转。面积、碰撞与轮廓计算必须基于真实多边形，不得未经论证用包围盒替代。

## 5. 实现与目录约定

- 当前运行环境为 Python 3.12，使用 `pyproject.toml`、`uv.lock` 和 uv 管理依赖；直接优化模型使用 OR-Tools CP-SAT。
- `src/vlsi_floorplan/` 放解析、模型与输出实现，`tests/` 放自动化验证，`outputs/` 放可复算 JSON 与布局图，`ref/` 放参考文献。新目录仍应由实际职责驱动。
- 真实命令以 `README.md` 为准；最小验证入口为 `uv run python -m unittest discover -s tests -v`。
- 输入解析、几何可行性、目标函数、搜索算法、可视化和实验编排应保持职责分离；优化算法不得自行维护另一套几何或 HPWL 口径。
- 随机算法必须支持显式随机种子，并记录数据集、参数、终止条件、运行时间、可行性和目标值。
- 生成物应写入明确的输出目录，并能追溯到输入、配置和代码版本；不得只保留截图或无法复算的汇总数值。

## 6. 修改与验证要求

- 修改前阅读相关赛题页、数据样例、现有实现与测试；优先做最小可验证修改，不做无关重构。
- 新增或修改解析逻辑时，至少覆盖三种文件格式、三组数据数量校验、非法引用和截断输入。
- 新增或修改布局逻辑时，至少验证旋转后尺寸、非负坐标、两两不重叠、轮廓边界、面积和长宽比。
- 新增或修改线长逻辑时，至少用手算小例验证单网 HPWL、Terminal 与模块混合线网、旋转后模块中心。
- 新增或修改启发式算法时，报告可行率和多随机种子结果；单次最优结果不能代表稳定性能。
- 问题 3 的“最小死区比例”若仅由数值搜索获得，必须给出搜索精度、可行性判据和上下界，不得写成严格最优证明。
- 修改后检查是否需要同步更新 `README.md`、`docs/`、配置说明、测试和示例。无法运行验证时，必须说明静态检查和剩余风险。

## 7. Skills 使用策略

- 普通解析、建模、算法实现和测试默认不查看专项 skill。
- 读取或处理 PDF 时使用 PDF skill；撰写或修改论文时按交付格式使用 academic-paper、LaTeX、DOCX 或 PPTX 类 skill。
- 绘制论文级图表时按需使用 nature-figure；处理表格交付物时使用 spreadsheets skill。
- 需要整理代码边界时参考 `docs/代码组织.md`。当前离线数据优化问题通常不适用 Web 权限审计；若以后引入服务或外部不可信输入，再参考 `docs/代码审计.md`。
- skill 只补充专项流程，不得改变本文件和赛题原文定义。

## 8. 结果汇报

- 最终回复至少说明：改了什么、为什么这样改、如何验证、剩余风险或未覆盖项。
- 涉及实验结果时，分别报告数据校验、约束可行性、目标值、重复实验稳定性和可视化产物，避免把“程序运行成功”等同于“模型结论成立”。
